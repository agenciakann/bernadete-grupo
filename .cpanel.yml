---
deployment:
  tasks:
    # O repositorio esta clonado dentro do Document Root
    # (/home3/makefa42/lp2.bernadeteleite.com), entao o proprio
    # "git checkout" do deploy ja publica os arquivos no site.
    # Nao ha nada para copiar; esta tarefa so registra o deploy.
    - /bin/echo "Deploy HEAD concluido."
